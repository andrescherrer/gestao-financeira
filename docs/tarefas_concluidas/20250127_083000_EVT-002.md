# EVT-002 - Criar domain events para User (UserRegistered, etc.)

**Data:** 2025-01-27  
**Status:** ✅ Concluída

## Resumo da Implementação

**Tarefa:** EVT-002 - Criar domain events para User (UserRegistered, etc.)

### Arquivos Criados

1. **`backend/internal/identity/domain/events/user_registered.go`**
   - Evento de domínio `UserRegistered`
   - Contém informações do usuário (email, name)
   - Estende `BaseDomainEvent`

### Arquivos Modificados

1. **`backend/internal/identity/domain/entities/user.go`**
   - Modificado para usar `UserRegistered` event específico
   - Evento agora inclui email e nome do usuário

### Características

- ✅ Evento específico com informações detalhadas do usuário
- ✅ Inclui email e nome para logging e auditoria
- ✅ Implementa interface `DomainEvent`
- ✅ Substitui evento genérico por evento específico

### Estrutura do Evento

```go
type UserRegistered struct {
    events.BaseDomainEvent
    email string
    name  string
}
```

### Métodos

- `Email()` - Retorna o email do usuário
- `Name()` - Retorna o nome completo do usuário

### Integração

- ✅ Evento criado automaticamente na entidade `User` ao criar novo usuário
- ✅ Publicado via Event Bus quando usuário é registrado
- ✅ Pode ser usado para logging, auditoria, e outros handlers

### Commits Realizados

- `feat(backend): EVT-002 - criar domain events para User`

### Próxima Tarefa

EVT-003 - Criar domain events para Account (AccountCreated, etc.)

